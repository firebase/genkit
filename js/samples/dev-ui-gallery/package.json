{
  "name": "dev-ui-gallery",
  "version": "1.0.0",
  "description": "Loads plugins and flows to demonstrate and test UI features.",
  "main": "lib/index.js",
  "scripts": {
    "start": "node lib/index.js",
    "compile": "tsc",
    "build": "pnpm build:clean && pnpm compile",
    "build:clean": "rm -rf ./lib",
    "build:watch": "tsc --watch",
    "build-and-run": "pnpm build && node lib/index.js"
  },
  "keywords": [],
  "author": "Google, LLC",
  "license": "ISC",
  "devDependencies": {
    "typescript": "^5.3.3"
  },
  "dependencies": {
    "@genkit-ai/ai": "workspace:*",
    "@genkit-ai/core": "workspace:*",
    "@genkit-ai/dotprompt": "workspace:*",
    "@genkit-ai/flow": "workspace:*",
    "@genkit-ai/chromadb": "workspace:*",
    "@genkit-ai/dev-local-vectorstore": "workspace:*",
    "@genkit-ai/firebase": "workspace:*",
    "@genkit-ai/googleai": "workspace:*",
    "@genkit-ai/ollama": "workspace:*",
    "@genkit-ai/pinecone": "workspace:*",
    "@genkit-ai/ragas": "workspace:*",
    "@genkit-ai/vertexai": "workspace:*",
    "zod": "^3.22.4"
  }
}
