{
  "scripts": {
    "build": "(cd functions && npm run install-dist  && npm i && npm run build) && (npm run build:js)",
    "build:js": "webpack"
  },
  "private": true,
  "dependencies": {
    "firebase": "^10.9.0",
    "jquery": "^3.7.1"
  },
  "devDependencies": {
    "webpack": "^5.90.3",
    "webpack-cli": "^5.1.4"
  }
}
