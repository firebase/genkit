# Prompts

Dotprompt: `.prompt` files with templates, schemas, partials. [Docs](https://genkit.dev/docs/dotprompt).

```bash
export GEMINI_API_KEY=your-api-key
uv sync
genkit start -- uv run src/main.py
```

Dev UI at http://localhost:4000. Try `chef_flow`, `tell_story`.
