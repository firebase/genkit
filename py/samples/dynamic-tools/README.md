# Dynamic Tools

`ai.dynamic_tool()` and `ai.run()` — register tools at runtime, trace arbitrary functions.

```bash
export GEMINI_API_KEY=your-api-key
uv sync
genkit start -- uv run src/main.py
```

Dev UI at http://localhost:4000.
