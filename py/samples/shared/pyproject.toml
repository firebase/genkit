[project]
name = "shared"
version = "0.0.1"
description = "Shared sample code"
requires-python = ">=3.10"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["flows.py"]
