{
  "private": true,
  "version": "0.0.1",
  "workspaces": [
    "./*",
    "./plugins/*",
    "./samples/*"
  ],
  "scripts": {
    "lint": "eslint --ext .ts .",
    "lint:fix": "eslint --fix --ext .ts .",
    "install-parent-deps": "npm i",
    "build:all": "npm-run-all build:common build:flow build:ai build:providers build:plugins build:dotprompt pack:all build:samples",
    "build:common": "cd common && npm install && npm run build",
    "build:flow": "cd flow && npm install && npm run build",
    "build:ai": "cd ai && npm install && npm run build",
    "build:providers": "cd providers && npm install && npm run build",
    "build:dotprompt": "cd dotprompt && npm install && npm run build",
    "build:plugins": "for i in plugins/*/; do cd $i && npm install && npm run build && cd ../..; done",
    "build:samples": "pwd && for i in samples/*/; do cd $i && npm install && npm run build && cd ../..; done",
    "pack:all": "rm -r dist || true && mkdir dist && npm-run-all pack:common pack:flow pack:ai pack:providers zip:dist",
    "pack:common": "cd common && npm pack --pack-destination ../dist -ws",
    "pack:flow": "cd flow && npm pack --pack-destination ../dist -ws",
    "pack:ai": "cd ai && npm pack --pack-destination ../dist -ws",
    "pack:providers": "cd providers && npm pack --pack-destination ../dist -ws",
    "zip:dist": "cd dist && zip genkit-dist-0.0.1.zip *.tgz"
  },
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "^5.12.0",
    "@typescript-eslint/parser": "^5.12.0",
    "eslint": "^8.9.0",
    "eslint-config-google": "^0.14.0",
    "eslint-config-prettier": "^8.3.0",
    "eslint-plugin-import": "^2.25.4",
    "eslint-plugin-jsdoc": "^39.2.9",
    "eslint-plugin-prettier": "^4.0.0",
    "prettier": "^2.7.1",
    "typescript": "^4.9.0",
    "npm-run-all": "^4.1.5"
  }
}
