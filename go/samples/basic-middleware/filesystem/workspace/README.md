# Acme Weather Service

A tiny REST API that exposes current weather for a given city.
This is sample content for the Genkit filesystem-middleware demo.

## Endpoints

- `GET /weather/{city}` — returns temperature and conditions.
- `GET /health` — liveness probe.

## Status

Pre-release. Tracking open work in `todo.txt`.
